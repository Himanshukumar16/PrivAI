# PrivAI Local Microservice Properties
spring.application.name=privai-backend
server.port=8080
server.address=127.0.0.1

# Strict local CORS setup
privai.allowed-origins=*

# Vault Retention (Minutes)
privai.vault.ttl-minutes=120
